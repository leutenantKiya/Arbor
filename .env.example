# --- Database (Neon Postgres, via Vercel Marketplace integration) ---
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# --- Session signing (generate: openssl rand -base64 32) ---
AUTH_SECRET=""

# --- Particle Network (dashboard.particle.network) ---
NEXT_PUBLIC_PARTICLE_PROJECT_ID=""
NEXT_PUBLIC_PARTICLE_CLIENT_KEY=""
NEXT_PUBLIC_PARTICLE_APP_ID=""
# Project Settings > Server Key — server-only, never NEXT_PUBLIC.
# Used to verify {uuid, token} via Particle's getUserInfo RPC (ARCHITECTURE.md §8).
PARTICLE_SERVER_KEY=""

# --- Chain ---
# ethereum-sepolia (testnet demo, moved off arbitrum-sepolia — AA was chain-gated
# on Particle's backend for this project) | arbitrum (mainnet, Universal Accounts)
NEXT_PUBLIC_CHAIN="ethereum-sepolia"
NEXT_PUBLIC_ARBITRUM_CHAIN_ID=11155111
NEXT_PUBLIC_ARBORVAULT_ADDRESS="0xBf7620758c6db08b4822a05f39734198d60E7Fe3"
NEXT_PUBLIC_USDC_ADDRESS="0xA3443AdA370C8ea8Bd3838E282D164649D02863B"

# --- Settlement signer (server-only; testnet funds only; never NEXT_PUBLIC) ---
SETTLEMENT_PRIVATE_KEY=""
