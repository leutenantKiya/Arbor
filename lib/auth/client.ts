'use client';

// Client-side auth utilities — session data is read from the server
// via server components / server actions. No client-side auth SDK needed.
// Use the Nav component's session prop or call server actions directly.
export {};